Stick any javascript you want in here so that when a new version is released, the
customers get the latest version instead of the one in the cache.  Our res/js
folder is renamed to res<version>/js so that it gets the latest versions instead
of the ones from the cache.